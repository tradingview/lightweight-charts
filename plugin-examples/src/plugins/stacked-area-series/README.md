# Stacked area series

This plugin has graduated to a published package,
[`@tradingview/lwc-plugin-stacked-area-series`](https://www.npmjs.com/package/@tradingview/lwc-plugin-stacked-area-series).
Its source lives in [`packages/lwc-plugin-stacked-area-series`](../../../../packages/lwc-plugin-stacked-area-series).

The `example/` folder here is the gallery page, which imports the package.
