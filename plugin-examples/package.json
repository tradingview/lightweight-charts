{
    "name": "lightweight-charts-plugin-examples",
    "private": true,
    "type": "module",
    "scripts": {
        "build:utils": "pnpm --filter @tradingview/lwc-plugin-utils build",
        "build": "pnpm build:utils && tsc",
        "compile": "pnpm build:utils && tsc && node compile.mjs",
        "dev": "pnpm build:utils && vite --config src/vite.config.js",
        "build:examples:site": "pnpm build:utils && vite build --config src/vite.config.js && node build-website.mjs"
    },
    "devDependencies": {
        "typescript": "5.5.4",
        "vite": "7.2.0"
    },
    "dependencies": {
        "@tradingview/lwc-plugin-utils": "workspace:*",
        "dts-bundle-generator": "9.5.1",
        "fancy-canvas": "2.1.0",
        "globby": "14.1.0",
        "lightweight-charts": "workspace:*"
    }
}
