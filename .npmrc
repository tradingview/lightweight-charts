# Resolve packages from the public npm registry so that lockfile entries
# never embed URLs of private registry mirrors configured in a user-level
# ~/.npmrc (such entries break installs for CI and external contributors).
registry=https://registry.npmjs.org/
