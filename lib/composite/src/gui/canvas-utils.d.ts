import { CanvasElementBitmapSizeBinding, Size } from 'fancy-canvas';
export declare function createBoundCanvas(parentElement: HTMLElement, size: Size): CanvasElementBitmapSizeBinding;
export declare function releaseCanvas(canvas: HTMLCanvasElement): void;
